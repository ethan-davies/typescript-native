{
  "name": "sonite",
  "version": "0.0.0",
  "private": true,
  "description": "A TypeScript-like language that compiles to native code via LLVM",
  "license": "MIT",
  "author": "Ethan Davies",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean",
    "dev": "pnpm --filter @sonite/compiler build && pnpm --filter @sonite/runtime build && pnpm --filter @sonite/std build && tsx packages/cli/src/cli.ts",
    "typecheck": "pnpm --filter @sonite/compiler build && pnpm -r run typecheck",
    "test": "pnpm --filter @sonite/compiler test && pnpm --filter @sonite/runtime test && pnpm --filter @sonite/lsp test && pnpm --filter sonite-vscode test",
    "test:watch": "pnpm --filter @sonite/compiler test:watch",
    "test:coverage": "pnpm --filter @sonite/compiler test:coverage"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@types/node": "^22.15.30",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
