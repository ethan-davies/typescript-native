/** Nullable helpers — prefer `== null` / `!= null` for narrowing. */

export function isNull<T>(value: T | null): bool {
  return value == null;
}

export function isNotNull<T>(value: T | null): bool {
  return value != null;
}
