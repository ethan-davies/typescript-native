export class Duration {
  private millis: i64;

  constructor(millis: i64) {
    this.millis = millis;
  }

  toMillis(): i64 {
    return this.millis;
  }

  static fromMillis(ms: i64): Duration {
    return new Duration(ms);
  }
}
